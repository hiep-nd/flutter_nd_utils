name: nd_utils
description: A utility library.
version: 1.0.4
homepage: https://github.com/hiep-nd/flutter_nd_utils

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.17.0
  logging: ^1.0.0
  
  nd_core_utils: ^1.0.20
    # path: ../nd_core_utils

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
